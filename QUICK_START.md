# PeakPicker クイックスタートガイド

## インストール（初回のみ）

### 1. Python環境の確認
```bash
python --version
```
Python 3.7以上が必要です。

### 2. 依存パッケージのインストール
```bash
pip install -r requirements.txt
```

## 起動方法

### Windows
`run_peakpicker.bat` をダブルクリック

### Linux/Mac
```bash
./run_peakpicker.sh
```

## 基本的な使い方

### サンプルデータで試す

1. **ファイルを開く**
   - 「ファイルを開く」ボタンをクリック
   - `C_0.6MeV_pos.mpa` を選択

2. **最初のピークを同定**
   - プロット上でピークの左端をクリック
   - ピークの右端をクリック
   - 「選択範囲を自動フィット」ボタンをクリック
   - m/z値を入力（例：1 for H+）

3. **2番目のピークを同定**
   - 別のピークで同様の操作を実行
   - 異なるm/z値を入力（例：12 for C+）

4. **キャリブレーション**
   - 「m/z キャリブレーション」ボタンをクリック
   - A, B, R²の値が表示される

5. **m/z軸で表示**
   - X軸を「m/z」に切り替え
   - スペクトルがm/z軸で表示される

6. **自動ピーク検出**
   - 検出したい範囲を2点クリックで選択
   - 「範囲内ピーク自動検出＋フィット」ボタンをクリック
   - 検出されたピークが自動的にフィット・同定される

7. **結果をエクスポート**
   - 「結果をエクスポート」ボタンをクリック
   - ファイル名を指定して保存

## よく使う機能

### ズーム
- **ズームイン**: 表示を拡大
- **ズームアウト**: 表示を縮小
- **全体表示**: スペクトル全体を表示

### ベースライン減算
1. BG減算欄に値を入力（例：50）
2. 「実行」をクリック
3. 元に戻す場合は空欄にして「実行」

### ピーク削除
1. テーブル上で削除したいピークをクリック
2. 「選択ピーク削除」ボタンをクリック

### 積算モード
1. 解析モードで「積算」を選択
2. プロット上で範囲を2点クリック
3. 自動的に積算される

## トラブルシューティング

### パッケージのインストールエラー
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### ファイルが開けない
- ファイルが破損していないか確認
- UTF-8エンコーディングで保存されているか確認

### フィットが失敗する
- 選択範囲を広めに取り直す
- BG減算を試す

## 詳細情報

詳しい使い方は `README.md` を参照してください。

---
**PeakPicker v2.0**
