# マウス操作中心のUIコンセプト

スペクトル上での直感的なピーク選択と判定を重視したUIの仕様案をまとめています。通常モードではブラウズと軽い編集を同一モードで扱い、必要最小限の場合にのみ専用モード（例：レビュー専用モード）へ切り替えます。

## 基本操作方針
- 左クリックで選択、ドラッグで範囲選択、右クリックでコンテキスト操作。
- 主要操作（ピーク採用/却下、範囲指定、フィット、ズームなど）はグラフ上で完結させる。
- 候補ピークは自動検出後に「proposed/accepted/rejected」の状態を持ち、色や線種で区別する。

## マウス操作
### 左クリック（単発）
- クリック位置に最も近いピーク候補（縦線）を選択。
- 選択されたピークは縦線色を変更（例：青→オレンジ）、ラベルを強調表示し、ピーク一覧テーブル側でも選択行を同期表示。

### 左ドラッグ（範囲選択）
- ドラッグ開始から終了まで薄い矩形を表示。
- ドラッグ完了時にポップアップ/下部ボタンで操作を提示：
  - この範囲をズーム
  - この範囲のピークを自動検出＋フィット
  - この範囲をROIとして保存
  - 将来的には範囲内候補ピークの一括採用/一括却下も追加

### ホイール
- マウスホイール：カーソル位置を中心にx軸ズームイン/アウト。
- Ctrl + ホイール：縦軸（カウント軸）のスケール変更（感度調整）。

### 右クリック（コンテキストメニュー）
- 右クリック位置に応じてメニュー内容を切替。
- ピーク候補上：
  - このピークを採用
  - このピークを却下
  - このピークを単峰フィット
  - このピークと近傍を多峰フィット
  - このピークにジャンプ（中心ズーム）
- ピーク候補のない場所：
  - ここを中心にズームイン
  - ここから指定幅で範囲を作成
  - この位置を背景基準点に登録

## ピーク候補の状態と操作
### 表示スタイル
- proposed（未判定）：青の点線。
- accepted（採用）：濃い実線。
- rejected（却下）：グレーの点線または非表示。
- フィット済みピークは生データ（細線）にフィット曲線（太線・色分け）を重ねる。

### 採用/却下操作
- ダブルクリック：proposed ↔ accepted をトグル。
- Shift + ダブルクリック：proposed/accepted → rejected。
- テーブル側のチェックボックスでも同等の状態変更が可能。

### レビュー専用モード（任意）
- 「ピークレビュー」ボタンで専用モードへ。最初の候補に自動ズーム。
- キーボードショートカット：A（採用）、D（却下）、S（スキップ）、←/→（前後候補）。
- 画面上に残り候補数やスコアを表示。

## 画面レイアウト案
- 上：メインプロット（スペクトル、凡例、タイトル）。
- 下：操作バー（全体表示、X軸切替、BG補正、範囲内ピーク自動検出＋フィット、単峰フィット、多峰フィット）。
- 右：ピーク一覧テーブル（採用フラグ、ID、TOF/m/z、Height、FWHM、Area、χ²/残差）。行クリックでズーム＋ハイライト、列ソート対応。

## 想定ユーザーフロー
### フロー1：一括自動検出→人間レビュー→採用ピーク出力
1. ドラッグで解析範囲を指定。
2. 「範囲内ピーク自動検出＋フィット」を実行し、候補（青点線）とフィット曲線を表示。テーブルへ一覧も出力。
3. グラフ上で良質なピークをダブルクリックしてaccepted、不適なものはShift+ダブルクリックでrejected。
4. 「採用ピークのみCSV出力」でレポート生成。

### フロー2：単峰を丁寧にフィット
1. グラフでピーク近傍をクリックして候補を選択。
2. 右クリックメニューから「このピークを単峰フィット」を実行。
3. フィット曲線とパラメータを表示。必要に応じて再フィットや手動調整を行い、採用/却下を判断。

## 依頼時の説明例
> グラフ上のマウス操作を中心に、左クリック/ドラッグによるピーク選択・範囲選択、右クリックメニューによるフィットと採用/却下、ダブルクリックやショートカットでの採否トグルを実装し、自動検出ピークを候補として提示した上で直感的に採否判断できるUIを目指す。
